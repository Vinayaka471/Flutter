# GameCodes Admin Panel (Pro) - Starter
File-based PHP admin panel + REST API for managing game codes and categories.
Drop into a PHP server (XAMPP/LAMP). See `config.php` to configure paths and tokens.
